public class ArrayListReview {
    public static void main(String[] args) {

        /*
                ArrayList is resizable array, dynamic in size

                * Store only objects

                ArrayList<Type of object> myArray = new ArrayList<>();

                * add()
                * get()
                * set()
                * remove()

                Array vs ArrayList

                * Size: arraylist is dynamic, array is fixed
                * Elements it can store: arraylist - only objects, array - primitives and objects

                * Both array and arraylist work with indexes when retrieving/getting the certain element
         */

    }
}
